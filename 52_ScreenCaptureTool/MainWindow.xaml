<Window x:Class="ScreenCaptureTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="スクリーンキャプチャツール" Height="480" Width="640">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="全画面キャプチャ" Padding="12,4" Click="CaptureFullScreenButton_Click" />
            <Button Content="範囲選択キャプチャ" Padding="12,4" Margin="8,0,0,0" Click="StartRegionSelectButton_Click" />
            <Button x:Name="SaveButton" Content="保存" Padding="12,4" Margin="8,0,0,0" Click="SaveButton_Click" />
            <Button x:Name="CopyButton" Content="クリップボードにコピー" Padding="12,4" Margin="8,0,0,0" Click="CopyButton_Click" />
        </StackPanel>

        <TextBlock x:Name="StatusText" Grid.Row="1" Margin="0,0,0,12"
                   AutomationProperties.AutomationId="StatusText" />

        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1">
            <Image x:Name="PreviewImageControl" Stretch="Uniform"
                   AutomationProperties.AutomationId="PreviewImage" />
        </Border>
    </Grid>
</Window>
