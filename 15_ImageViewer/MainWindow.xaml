<Window x:Class="ImageViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImageViewer"
        xmlns:converters="clr-namespace:ImageViewer.Converters"
        mc:Ignorable="d"
        Title="画像ビューア" Height="600" Width="900">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- フォルダ選択 -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="フォルダを選択" Padding="12,4" Margin="0,0,8,0"
                    Command="{Binding BrowseFolderCommand}"
                    AutomationProperties.AutomationId="BrowseFolderButton"/>
            <TextBlock Grid.Column="1" Text="{Binding SelectedFolderPath}" VerticalAlignment="Center"
                       TextTrimming="CharacterEllipsis"
                       AutomationProperties.AutomationId="SelectedFolderPathText"/>
        </Grid>

        <!-- ローディング表示 -->
        <ProgressBar Grid.Row="1" Margin="0,8,0,0" Height="4" IsIndeterminate="True"
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                     AutomationProperties.AutomationId="LoadingProgressBar"/>

        <!-- エラー表示 -->
        <TextBlock Grid.Row="2" Margin="0,8,0,0" Foreground="#C62828" TextWrapping="Wrap"
                   Text="{Binding ErrorMessage}"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"
                   AutomationProperties.AutomationId="ErrorMessageText"/>

        <!-- サムネイル一覧 + プレビュー -->
        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ListBox Grid.Column="0" Margin="0,0,8,0"
                     ItemsSource="{Binding ImageFiles}"
                     SelectedItem="{Binding SelectedImage, Mode=TwoWay}"
                     AutomationProperties.AutomationId="ThumbnailsListBox">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Width="200"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Width="90" Margin="4">
                            <Image Source="{Binding Thumbnail}" Width="80" Height="80" Stretch="Uniform"/>
                            <TextBlock Text="{Binding FileName}" FontSize="10" TextTrimming="CharacterEllipsis"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <DockPanel Grid.Column="1">
                <Grid DockPanel.Dock="Bottom" Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="◀ 前へ" Padding="12,4"
                            Command="{Binding PreviousCommand}"
                            AutomationProperties.AutomationId="PreviousButton"/>
                    <TextBlock Grid.Column="1" Text="{Binding SelectedImage.FileName}" HorizontalAlignment="Center"
                               VerticalAlignment="Center" AutomationProperties.AutomationId="PreviewFileNameText"/>
                    <Button Grid.Column="2" Content="次へ ▶" Padding="12,4"
                            Command="{Binding NextCommand}"
                            AutomationProperties.AutomationId="NextButton"/>
                </Grid>
                <Border Background="#F5F5F5">
                    <Image Source="{Binding SelectedImage.FilePath}" Stretch="Uniform" Margin="8"
                           AutomationProperties.AutomationId="PreviewImage"/>
                </Border>
            </DockPanel>
        </Grid>
    </Grid>
</Window>
