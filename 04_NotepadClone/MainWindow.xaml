<Window x:Class="NotepadClone.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NotepadClone"
        mc:Ignorable="d"
        Title="無題 - メモ帳" Height="450" Width="640"
        Closing="MainWindow_Closing">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="ファイル(_F)">
                <MenuItem Header="新規作成(_N)" Click="New_Click"
                          AutomationProperties.AutomationId="NewMenuItem"/>
                <MenuItem Header="開く(_O)..." Click="Open_Click"
                          AutomationProperties.AutomationId="OpenMenuItem"/>
                <Separator/>
                <MenuItem Header="保存(_S)" Click="Save_Click"
                          AutomationProperties.AutomationId="SaveMenuItem"/>
                <MenuItem Header="名前を付けて保存(_A)..." Click="SaveAs_Click"
                          AutomationProperties.AutomationId="SaveAsMenuItem"/>
            </MenuItem>
        </Menu>
        <TextBox x:Name="EditorTextBox"
                 AcceptsReturn="True" AcceptsTab="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="14"
                 TextChanged="EditorTextBox_TextChanged"
                 AutomationProperties.AutomationId="EditorTextBox"/>
    </DockPanel>
</Window>
