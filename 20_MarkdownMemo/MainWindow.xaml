<Window x:Class="MarkdownMemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MarkdownMemo"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:converters="clr-namespace:MarkdownMemo.Converters"
        mc:Ignorable="d"
        Title="Markdownメモ" Height="640" Width="1000">
    <Window.Resources>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ツールバー -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Margin="0,0,8,0" VerticalContentAlignment="Center"
                     Text="{Binding InputTitle, UpdateSourceTrigger=PropertyChanged}"
                     AutomationProperties.AutomationId="InputTitleTextBox"/>
            <Button Grid.Column="1" Content="新規" Padding="12,4" Margin="0,0,8,0"
                    Command="{Binding NewCommand}"
                    AutomationProperties.AutomationId="NewButton"/>
            <Button Grid.Column="2" Content="保存" Padding="12,4" Margin="0,0,8,0"
                    Command="{Binding SaveCommand}"
                    AutomationProperties.AutomationId="SaveButton"/>
            <Button Grid.Column="3" Content="削除" Padding="12,4"
                    Command="{Binding DeleteCommand}"
                    AutomationProperties.AutomationId="DeleteButton"/>
        </Grid>

        <!-- エラー表示 -->
        <TextBlock Grid.Row="1" Margin="0,0,0,8" Foreground="#C62828" TextWrapping="Wrap"
                   Text="{Binding ErrorMessage}"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"
                   AutomationProperties.AutomationId="ErrorMessageText"/>

        <!-- 一覧 / 編集 / プレビュー -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ListBox Grid.Column="0" ItemsSource="{Binding Memos}"
                      SelectedItem="{Binding SelectedMemo, Mode=TwoWay}"
                      DisplayMemberPath="Title"
                      AutomationProperties.AutomationId="MemosListBox"/>

            <TextBox Grid.Column="2"
                     Text="{Binding MarkdownContent, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True" AcceptsTab="True" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas" FontSize="14"
                     AutomationProperties.AutomationId="MarkdownContentTextBox"/>

            <wv2:WebView2 x:Name="PreviewWebView" Grid.Column="4"
                          AutomationProperties.AutomationId="PreviewWebView"/>
        </Grid>
    </Grid>
</Window>
